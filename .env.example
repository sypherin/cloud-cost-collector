# Copy to .env and source it (or export in your service unit). Never commit .env.

# Telegram sink
CCC_TELEGRAM_TOKEN=""
CCC_TELEGRAM_CHAT_ID=""

# Webhook sink (book expenses into your accounting/CRM)
CCC_WEBHOOK_URL=""
# CCC_WEBHOOK_AUTH=""   # if your endpoint needs an Authorization header
